<% if(nondef && _.templateHelpers.isAllowed('services', 'update')) { %>
<button type="button" class="removeRevision btn btn-default btn-xs" data-toggle="tooltip" title="<%= _.t('remove') %>">
    <span class="glyphicon glyphicon-remove"></span>
</button>
<% } %>
